services:
  lint:
    image: buildkite/plugin-linter
    command: ['--id', 'Nhattd97/kobiton-execute-test']
    volumes:
      - ".:/plugin:ro"
  tests:
    image: buildkite/plugin-tester
    volumes:
      - ".:/plugin:ro"
